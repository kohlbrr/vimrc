syntax on           " Turns on syntax hilight::ing (default)

set number          " Turns on line numbers

set tabstop=2       " Sets tabs to two spaces
set shiftwidth=2    " How many colums are indented when re-indenting
set expandtab       " Tabs are spaces
set smarttab        " Tab smarter

set list            " Show whitespace, tabs, and EOL

set ignorecase      " Ignore case in search patterns..
set smartcase       " ..but be smart about it
